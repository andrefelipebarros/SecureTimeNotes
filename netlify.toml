[build]
  command = "cd securetimenotes-frontend/securetimenotes && npm install && ng build --configuration=production --base-href /"
  publish = "../../securetimenotes/browser"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
